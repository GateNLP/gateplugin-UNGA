Phase: Lookup
Input: Lookup

Options: control = all

Rule: Lookup
({Lookup.majorType=="unbis"}):match
-->
:match.Unbis = {rule = "Lookup",
                label = :match.Lookup.pref_label,
                class = :match.Lookup.uri
               }

Rule: Lookup1
({Lookup.majorType=="un", Lookup.minorType=="tga"}):match
-->
:match.TGA = {rule = "Lookup1"}
