Phase: Preamble
Input: Title TGA Header Footer Token ParagraphNumber Operative
Options: control = appelt

Rule: Preamble0
( {Title}
  {TGA}
  (
    ({Token, !Footer, !Header, !ParagraphNumber, !Operative})+
  ):match
)
-->
:match.Preamble = {rule = "Preamble0"}


Rule: Preamble1
( {Header}
  (
    ({Token, !Footer, !Header, !ParagraphNumber, !Operative})+
  ):match
)
-->
:match.Preamble = {rule = "Preamble1"}
